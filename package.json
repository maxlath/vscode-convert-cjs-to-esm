{
  "name": "vscode-convert-cjs-to-esm",
  "displayName": "Command to convert `require` to an static `import`",
  "version": "1.0.0",
  "main": "extension.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "publisher": "maxlath",
  "author": "maxlath",
  "activationEvents": [
    "*"
  ],
  "license": "MIT",
  "dependencies": {
    "lodash.camelcase": "^4.3.0",
    "vscode": "^1.1.37"
  },
  "engines": {
    "vscode": "^1.18.0"
  },
  "devDependencies": {},
  "description": "Command to convert `require` to an static `import`"
}
